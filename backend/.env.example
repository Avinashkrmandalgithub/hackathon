# 🍃 MongoDB Configuration
DB_NAME=OrganDonationSystem
MONGODB_URI=mongodb://localhost:27017

# For MongoDB Atlas (cloud), replace above with:
# MONGODB_URI=mongodb+srv://hackathonuser:<password>@cluster0.xxxxx.mongodb.net/OrganDonationSystem?retryWrites=true&w=majority

# 🔐 JWT Configuration
ACCESS_TOKEN_SECRET=your-super-secret-access-token-key-for-hackathon-2024-change-this-in-production
REFRESH_TOKEN_SECRET=your-super-secret-refresh-token-key-for-hackathon-2024-change-this-too
ACCESS_TOKEN_EXPIRY=1d
REFRESH_TOKEN_EXPIRY=30d

# 🚀 Server Configuration
PORT=8000
NODE_ENV=development

# 🌐 CORS Configuration
CORS_ORIGIN=http://localhost:5173

# 📧 Email Configuration (Optional - for notifications)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-password

# 📱 SMS Configuration (Optional - for notifications)
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# ⚠️  IMPORTANT NOTES:
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env file to git (it's in .gitignore)
# 3. For production, use strong secrets and secure MongoDB Atlas connection
# 4. Share this template with team members, not the actual .env file
